# Source material — keep local, don't publish
books/
excerpts/

# Portable plaintext builds embed chapter excerpts. Generate locally.
# Only the encrypted department copy (private/reader.html) is committed.
portable/

# StatiCrypt local config (may include a salt; never put the password here)
.staticrypt.json

# Downloaded podcast audio and video files — personal use only, never publish
media/

# macOS
.DS_Store

# Editor cruft
.vscode/
*.swp
*~
